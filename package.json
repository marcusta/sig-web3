{
  "name": "sig-web3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node build.js --watch --serve",
    "build": "node build.js",
    "build:preview": "BASE_PATH=/preview node build.js",
    "build:prod": "BASE_PATH= node build.js"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.0",
    "nunjucks": "^3.2.4"
  }
}
